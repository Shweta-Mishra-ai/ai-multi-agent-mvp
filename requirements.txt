streamlit>=1.30
openai>=1.40
python-dotenv>=1.0
typer>=0.12
rich>=13.0
requests>=2.31
ddgs>=9.0
fastapi>=0.110
uvicorn>=0.29

# Optional (not required to run AgentOS - see README's Production
# hardening section): pip install sentry-sdk to enable SENTRY_DSN
# error monitoring.
